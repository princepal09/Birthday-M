// ─── Application Constants ───────────────────────────────────────────────────

/** Duration the loader screen is visible (ms) */
export const LOADER_DURATION = 2800;

/** The romantic quote displayed via typewriter */
export const ROMANTIC_QUOTE =
  "Out of all the days in the year, today is my favourite❤️ Because It gave you to me.";

/** Her name — the centerpiece of the reveal */
export const HER_NAME = "Happy Birthday My Love";

/** Typewriter speed: delay between each character (ms) */
export const TYPEWRITER_SPEED = 65;

/** Number of floating hearts (keep low for performance) */
export const HEART_COUNT = 12;

/** Number of star dots in the background */
export const STAR_COUNT = 40;

/** Steps in the cinematic intro flow */
export const STEPS = {
  LOADER: "loader",
  QUOTE: "quote",
  NAME: "name",
  BUTTON: "button",
};

/** Delay before background music starts on surprise page (ms) */
export const MUSIC_DELAY = 5000;
